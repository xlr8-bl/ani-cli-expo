import { StubScreen } from '@/components/StubScreen';

export default function Settings() {
  return (
    <StubScreen
      title="Settings"
      icon="settings-outline"
      note="Theme, quality, privacy & backup"
    />
  );
}
