import { StubScreen } from '@/components/StubScreen';

export default function Search() {
  return (
    <StubScreen
      title="Search"
      icon="search-outline"
      note="Debounced AniList search + genre chips"
    />
  );
}
