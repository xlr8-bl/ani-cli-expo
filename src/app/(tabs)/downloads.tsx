import { StubScreen } from '@/components/StubScreen';

export default function Downloads() {
  return (
    <StubScreen
      title="Downloads"
      icon="download-outline"
      note="Per-show offline downloads, grouped by series"
    />
  );
}
